{
  "$schema": "https://opencode.ai/config.json",
  "_comment": "Sandbox cell OpenCode config (see specs/04-worker-cell.md). Delivered via OPENCODE_CONFIG env. Auth comes from the attached OpenShell provider (OPENCODE_API_KEY, placeholder-masked) — no keys here. Set model to an ID from your OpenCode subscription catalog (Go or Zen); verify with `opencode models`. Per-run override: `opencode run --model <id>`.",
  "model": "opencode-go/kimi-k2.5",
  "small_model": "opencode-go/glm-5"
}
